@model IEnumerable<ConsumeApi.Models.CategoryDTO>

@{
    ViewData["Title"] = "GetAllCategories";
}

<h1>GetAllCategories</h1>

<p>
    <a asp-action="CreateCategory">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "UpdateCategory", new {  id=item.Id }) |
                    @Html.ActionLink("Details", "CategoryDetail", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "DeleteCategory", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
